import Loader from "@/components/share/Loader";
import React from "react";

const TestLaoding = async () => {
  return (
    <React.Fragment>
      <main>
        <Loader />
      </main>
    </React.Fragment>
  );
};

export default TestLaoding;
